-r ../backend/requirements.txt
# pywebview is installed separately with --no-deps so Windows packaging does not pull pythonnet.
pyinstaller==6.16.0
QtPy==2.4.3
PySide6==6.10.2
bottle==0.13.4
proxy_tools==0.1.0
